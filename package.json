{
  "name": "bun-async-disk-api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "release": "bun build --entrypoints ./src/server.ts ./src/flockAsyncWorker.ts --compile --minify --sourcemap",
    "dev": "bun run ./src/server"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "zod": "^4.1.4"
  }
}