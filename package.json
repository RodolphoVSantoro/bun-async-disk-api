{
  "name": "bun-async-disk-api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "release": "bun build --entrypoints ./src/server.ts --compile --minify --sourcemap",
    "dev": "bun run ./src/server",
    "reset-db": "bun run /scripts/resetDb.ts"
  },
  "devDependencies": {
    "@types/bun": "1.2.21"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "zod": "^4.1.4"
  }
}
